#include <numeric>
#include <vector>

#ifndef FUNCTIONS_H
#define FUNCTIONS_H

namespace func {
    int sum_multiples_in_vector(std::vector<int> range);
    bool is_divisible(int numerator, int denominator);
}

#endif